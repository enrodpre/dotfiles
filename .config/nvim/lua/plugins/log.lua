#!/usr/bin/lua

return {
   -- "enrodpre/logging.nvim",
   dir = "~/coding/logging",
   -- name = "logging",
   opts = {},
   -- dev = true,
   -- dir = "~/coding/nvim/plugins/logging.nvim",
   -- event = "VeryLazy",
   -- config = function(_, opts)
   --    require("logging.nvim").setup(opts)
   -- end,
}
